[package]
name = "replication-engine"
version = "0.1.0"
edition = "2024"

[workspace]
members = [
  "config",
  "membership",
  "node",
  "protocol",
  "runtime",
  "reconciliation",
  "replication",
]

[dependencies]
runtime = { path = "./runtime" }
