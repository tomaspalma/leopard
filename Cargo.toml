[package]
name = "replication-engine"
version = "0.1.0"
edition = "2024"

[workspace]
members = [
  "config",
  "membership",
  "node",
  "protocol",
  "runtime",
  "reconciliation",
  "replication",
]

[dependencies]
runtime = { path = "./runtime" }
node = { path = "./node" }
replication = { path = "./replication" }
log = "0.4.29"
dashmap = "6.1.0"
